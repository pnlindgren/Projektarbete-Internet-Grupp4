#include <stdio.h>

#include "main.h"
#include "clientFunctions.h"

void clientConnection()
{
    // Frågan är om det här behövs eller ej (Då det körs i uppkopplingen)
    /*if (SDLNet_Init() < 0)
	{
		fprintf(stderr, "SDLNet_Init: %s\n", SDLNet_GetError());
		exit(EXIT_FAILURE);
	}*/
}
